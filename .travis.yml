language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jperezsl/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jperezsl/docker-react npm run test
  